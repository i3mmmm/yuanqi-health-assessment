# 元炁源流健康评估系统 - 极简部署教程

## 🎯 快速开始（3步完成部署）

### 您需要确认的3件事：

1. ✅ 您有服务器的SSH访问权限（可以远程登录）
2. ✅ 您的服务器支持安装Node.js和MySQL
3. ✅ 您已准备好域名（或使用IP地址访问）

---

## 📝 第一步：上传文件到服务器

### 方法A：使用SCP命令（最快速）

**在您的本地电脑（Windows/Mac）打开终端/PowerShell，执行以下命令：**

```bash
# 替换成您的服务器IP地址
SERVER_IP="您的服务器IP"

# 1. 创建目标目录
ssh root@$SERVER_IP "mkdir -p /var/www/yuanqi-api /var/www/yuanqi-api/reports /var/www/yuanqi-frontend /root"

# 2. 上传后端核心文件（5个文件）
scp backend-api-server.js root@$SERVER_IP:/var/www/yuanqi-api/
scp database_schema.sql root@$SERVER_IP:/var/www/yuanqi-api/
scp data_migration.js root@$SERVER_IP:/var/www/yuanqi-api/
scp package.json root@$SERVER_IP:/var/www/yuanqi-api/
scp quick-deploy.sh root@$SERVER_IP:/root/

# 3. 上传前端文件（4个文件）
scp health_assessment_final_fixed.html root@$SERVER_IP:/var/www/yuanqi-frontend/
scp health_assessment_backend_complete.html root@$SERVER_IP:/var/www/yuanqi-frontend/
scp frontend-api-integration.js root@$SERVER_IP:/var/www/yuanqi-frontend/
scp admin-api-integration.js root@$SERVER_IP:/var/www/yuanqi-frontend/

# 4. 上传数据文件（4个文件）
scp symptoms_299_complete.json root@$SERVER_IP:/var/www/yuanqi-api/
scp first_assessment_20251213.json root@$SERVER_IP:/var/www/yuanqi-api/
scp second_assessment_20260113.json root@$SERVER_IP:/var/www/yuanqi-api/
scp sample_assessment_data.json root@$SERVER_IP:/var/www/yuanqi-api/

# 5. 上传品牌图片（4个PNG文件）
scp "元炁源流横版金色.png" root@$SERVER_IP:/var/www/yuanqi-frontend/
scp "元炁源流横版黑红色.png" root@$SERVER_IP:/var/www/yuanqi-frontend/
scp "元炁源流竖版金色.png" root@$SERVER_IP:/var/www/yuanqi-frontend/
scp "元炁源流竖版黑红色.png" root@$SERVER_IP:/var/www/yuanqi-frontend/
```

**说明：**
- 将 `您的服务器IP` 替换为实际的服务器IP地址
- 如果文件提示不存在，说明您的列表中没有该文件，可以跳过
- package.json 文件会在下一步自动创建，如果上传失败也没关系

---

### 方法B：使用SFTP工具（图形界面）

**推荐工具：FileZilla（免费）**

#### 1. 下载安装FileZilla
- 访问：https://filezilla-project.org/
- 下载并安装

#### 2. 连接到服务器
打开FileZilla，填写：
- 主机：`您的服务器IP`
- 端口：`22`
- 协议：`SFTP`
- 用户名：`root`
- 密码：`您的root密码`

点击"快速连接"

#### 3. 上传文件

连接成功后，在右侧（远程站点）：

**创建目录：**
- 右键点击空白处 → "创建目录" → 输入：`yuanqi-api`
- 进入 `yuanqi-api`，创建子目录：`reports`
- 返回根目录，再创建：`yuanqi-frontend`

**上传后端文件到 `/var/www/yuanqi-api/`：**
从左侧（本地站点）拖拽以下文件到右侧：
- `backend-api-server.js`
- `database_schema.sql`
- `data_migration.js`
- `quick-deploy.sh`
- `symptoms_299_complete.json`
- `first_assessment_20251213.json`
- `second_assessment_20260113.json`
- `sample_assessment_data.json`

**上传前端文件到 `/var/www/yuanqi-frontend/`：**
- `health_assessment_final_fixed.html`
- `health_assessment_backend_complete.html`
- `frontend-api-integration.js`
- `admin-api-integration.js`
- `元炁源流横版金色.png`
- `元炁源流横版黑红色.png`
- `元炁源流竖版金色.png`
- `元炁源流竖版黑红色.png`

---

## 🚀 第二步：运行一键部署脚本

### 1. 连接到服务器

```bash
ssh root@您的服务器IP
# 输入密码后登录
```

### 2. 赋予脚本执行权限

```bash
chmod +x /root/quick-deploy.sh
```

### 3. 运行部署脚本

```bash
bash /root/quick-deploy.sh
```

### 4. 按照提示输入信息

脚本会依次询问：

**问题1：请输入域名**
- 如果您有域名，输入：`yourdomain.com`
- 如果没有域名，输入：`您的服务器IP`

**问题2：请输入MySQL root密码**
- 输入一个强密码（至少8位，包含字母和数字）
- **请记住这个密码！**

**问题3：文件已上传完成? (y/n)**
- 输入：`y`

脚本将自动执行以下操作（大约15-30分钟）：
- ✅ 更新系统
- ✅ 安装Node.js 18
- ✅ 安装MySQL
- ✅ 安装Nginx
- ✅ 安装PM2
- ✅ 安装依赖包
- ✅ 配置环境变量
- ✅ 初始化数据库
- ✅ 执行数据迁移
- ✅ 配置Nginx反向代理
- ✅ 启动后端服务
- ✅ 配置防火墙

### 5. 等待部署完成

看到以下提示表示部署成功：
```
========================================
  部署完成！
========================================

重要信息：

MySQL root密码: xxxxxxxxxxxxxxxxxxxx
MySQL用户密码: xxxxxxxxxxxxxxxxxxxx

后端API地址: http://yourdomain.com/api/
前端评估页面: http://yourdomain.com/health_assessment_final_fixed.html
管理后台页面: http://yourdomain.com/health_assessment_backend_complete.html

默认管理员账号：
  用户名: admin
  密码: admin123
  ⚠ 请登录后立即修改密码！
```

**⚠️ 重要：请复制保存这些密码信息！**

---

## 🔧 第三步：修改前端配置（重要）

### 1. 编辑前端API配置文件

```bash
nano /var/www/yuanqi-frontend/frontend-api-integration.js
```

找到这一行（大约在第16行）：
```javascript
const API_CONFIG = {
    baseURL: 'http://localhost:3000',  // ← 修改这里
    // ...
};
```

修改为您的域名或IP：
```javascript
const API_CONFIG = {
    baseURL: 'http://yourdomain.com',  // 改为您的域名
    // 或者：baseURL: 'http://您的服务器IP',
};
```

保存并退出：
1. 按 `Ctrl + O`（保存）
2. 按 `Enter`（确认）
3. 按 `Ctrl + X`（退出）

### 2. 编辑后台API配置文件

```bash
nano /var/www/yuanqi-frontend/admin-api-integration.js
```

找到并修改（大约在第16行）：
```javascript
const AdminAPIConfig = {
    baseURL: 'http://localhost:3000',  // ← 修改这里
    // ...
};
```

修改为：
```javascript
const AdminAPIConfig = {
    baseURL: 'http://yourdomain.com',  // 改为您的域名
    // 或者：baseURL: 'http://您的服务器IP',
};
```

保存并退出（Ctrl+O，Enter，Ctrl+X）

---

## ✅ 第四步：测试部署

### 1. 检查服务状态

```bash
# 检查后端服务
pm2 status
```

应该看到：
```
┌─────┬───────────────┬─────────────┬─────────┬─────────┬──────────┐
│ id  │ name          │ version     │ mode    │ status  │ cpu      │
├─────┼───────────────┼─────────────┼─────────┼─────────┼──────────┤
│ 0   │ yuanqi-api    │ 18.17.0     │ fork    │ online  │ 0%       │
└─────┴───────────────┴─────────────┴─────────┴─────────┴──────────┘
```

```bash
# 检查Nginx状态
systemctl status nginx
```

应该显示：`active (running)`

### 2. 测试API接口

```bash
curl http://yourdomain.com/api/stats
```

应该返回JSON格式的统计数据。

### 3. 在浏览器中访问

**评估页面：**
```
http://yourdomain.com/health_assessment_final_fixed.html
```

**管理后台：**
```
http://yourdomain.com/health_assessment_backend_complete.html
```

**登录管理后台：**
- 用户名：`admin`
- 密码：`admin123`

### 4. 测试完整流程

1. 填写评估表单并提交
2. 查看是否成功提交到数据库
3. 登录管理后台
4. 查看评估记录
5. 生成PDF报告

---

## 🔐 可选：配置SSL证书（HTTPS）

### 配置HTTPS（推荐）

```bash
# 安装SSL证书
certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 按照提示输入邮箱地址并同意条款
```

### 配置自动续期

```bash
# 添加定时任务
(crontab -l 2>/dev/null; echo "0 3 * * * certbot renew --quiet") | crontab -
```

配置HTTPS后，需要修改前端配置：

```bash
nano /var/www/yuanqi-frontend/frontend-api-integration.js
```

将 `http://` 改为 `https://`：
```javascript
const API_CONFIG = {
    baseURL: 'https://yourdomain.com',  // 改为https
};
```

同样修改 `admin-api-integration.js`

---

## 📋 部署检查清单

部署完成后，请确认以下各项：

- [ ] 后端服务运行正常（`pm2 status` 显示 online）
- [ ] Nginx服务运行正常（`systemctl status nginx` 显示 active）
- [ ] MySQL服务运行正常（`systemctl status mysql` 显示 active）
- [ ] 可以通过浏览器访问评估页面
- [ ] 可以通过浏览器访问管理后台
- [ ] 可以正常登录管理后台（admin/admin123）
- [ ] 可以提交评估数据
- [ ] 可以在管理后台查看评估记录
- [ ] 可以生成PDF报告

---

## ❌ 常见问题解决

### 问题1：连接服务器时提示"Connection refused"

**原因：** 端口未开放或SSH服务未启动

**解决：**
```bash
# 检查SSH服务状态
systemctl status ssh

# 启动SSH服务
systemctl start ssh

# 检查防火墙
ufw status
sudo ufw allow 22/tcp
```

### 问题2：SCP上传文件时提示"No such file or directory"

**原因：** 服务器上目标目录不存在

**解决：**
```bash
# 在服务器上创建目录
ssh root@您的服务器IP
mkdir -p /var/www/yuanqi-api /var/www/yuanqi-frontend
exit
```

然后重新上传文件。

### 问题3：运行脚本时提示"Permission denied"

**原因：** 脚本没有执行权限

**解决：**
```bash
chmod +x /root/quick-deploy.sh
bash /root/quick-deploy.sh
```

### 问题4：部署后无法访问页面

**检查步骤：**

```bash
# 1. 检查Nginx配置
nginx -t

# 2. 检查Nginx错误日志
tail -f /var/log/nginx/error.log

# 3. 检查后端服务日志
pm2 logs yuanqi-api

# 4. 检查文件是否存在
ls -la /var/www/yuanqi-frontend/
ls -la /var/www/yuanqi-api/

# 5. 检查防火墙
ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
```

### 问题5：PM2服务显示"errored"

**解决：**
```bash
# 查看详细错误日志
pm2 logs yuanqi-api --lines 50

# 重启服务
pm2 restart yuanqi-api

# 如果仍有问题，检查依赖
cd /var/www/yuanqi-api
npm install
pm2 restart yuanqi-api
```

### 问题6：数据库连接失败

**检查步骤：**

```bash
# 1. 测试MySQL连接
mysql -u yuanqi_user -p
# 输入密码（部署时生成的MySQL用户密码）

# 2. 检查数据库是否存在
mysql -u yuanqi_user -p -e "SHOW DATABASES;"

# 3. 检查.env文件配置
cat /var/www/yuanqi-api/.env
```

---

## 📞 需要帮助？

如果遇到问题，请提供以下信息：

1. **错误信息**（截图或文字）
2. **您执行到哪一步**
3. **检查清单中哪些项未通过**
4. **相关日志输出**

我可以帮您排查问题。

---

## 🎉 部署成功后

恭喜！您的系统已成功部署。接下来可以：

1. **修改默认密码**
   - 登录管理后台
   - 修改admin密码

2. **配置SSL证书（HTTPS）**
   - 提高安全性
   - 提升用户信任

3. **设置自动备份**
   - 防止数据丢失
   - 定期备份数据库

4. **监控系统性能**
   - PM2监控：`pm2 monit`
   - Nginx日志：`tail -f /var/log/nginx/access.log`

5. **定期更新**
   - 更新系统：`apt update && apt upgrade`
   - 更新依赖：`npm update`

祝您使用愉快！
