# 元炁源流健康评估系统 - 网络空间部署指南

## 📋 部署前的准备

### 第一步：确认您的网络空间类型

根据您的网络空间类型，部署方式会有所不同。请确认您属于以下哪种情况：

#### 1. 虚拟主机（共享主机）
**特点：**
- 提供商：阿里云虚拟主机、腾讯云轻量应用服务器、Bluehost、HostGator等
- 提供cPanel/宝塔面板控制台
- 通常支持PHP、MySQL，但不一定支持Node.js
- 文件通过FTP/SFTP上传

#### 2. VPS/云服务器
**特点：**
- 提供商：阿里云ECS、腾讯云CVM、AWS EC2、DigitalOcean等
- 完整的服务器控制权（SSH root访问）
- 可以安装任何软件（Node.js、MySQL等）
- 需要自己配置环境

#### 3. 容器化部署
**特点：**
- 提供商：阿里云容器服务、腾讯云容器服务、Docker等
- 使用Docker容器部署
- 更灵活、易于扩展

#### 4. 静态网站托管
**特点：**
- 提供商：GitHub Pages、Vercel、Netlify、阿里云OSS等
- 仅支持静态HTML/CSS/JS文件
- **不支持**Node.js后端和MySQL数据库

---

## 🚀 部署方案选择

### 方案A：适用于VPS/云服务器（推荐）

**适用场景：** 您有完整的服务器控制权（SSH访问）

#### 步骤1：连接到您的服务器

```bash
# 通过SSH连接到服务器
ssh root@your_server_ip

# 或使用密钥连接
ssh -i your_key.pem root@your_server_ip
```

#### 步骤2：准备服务器环境

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Node.js 18.x LTS
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装MySQL
sudo apt install -y mysql-server
sudo mysql_secure_installation

# 安装Nginx
sudo apt install -y nginx

# 安装PM2（进程管理器）
npm install -g pm2

# 验证安装
node --version
mysql --version
nginx -v
pm2 --version
```

#### 步骤3：创建项目目录

```bash
# 创建项目目录
sudo mkdir -p /var/www/yuanqi-api
cd /var/www/yuanqi-api

# 上传项目文件（见下文详细说明）
```

#### 步骤4：上传文件到服务器

**方法1：使用SCP命令（推荐）**

在您的本地电脑终端执行：

```bash
# 上传单个文件
scp backend-api-server.js root@your_server_ip:/var/www/yuanqi-api/

# 上传整个文件夹
scp -r ./yuanqi-project/* root@your_server_ip:/var/www/yuanqi-api/
```

**方法2：使用Git（推荐用于持续更新）**

```bash
# 在服务器上
cd /var/www/yuanqi-api
git clone https://github.com/yourusername/yuanqi-project.git .
```

**方法3：使用SFTP工具（适合图形界面操作）**

推荐工具：
- **FileZilla**（Windows/Mac/Linux）
- **WinSCP**（Windows）
- **Cyberduck**（Mac）

FileZilla配置示例：
- 主机：your_server_ip
- 端口：22
- 协议：SFTP
- 用户名：root
- 密码：your_password

连接后，将文件拖拽到 `/var/www/yuanqi-api` 目录。

#### 步骤5：安装依赖和配置

```bash
cd /var/www/yuanqi-api

# 安装Node.js依赖
npm install --production

# 创建.env文件
nano .env
```

`.env` 文件内容：

```env
PORT=3000
NODE_ENV=production

# 数据库配置
DB_HOST=localhost
DB_USER=yuanqi_user
DB_PASSWORD=your_secure_password_here
DB_NAME=yuanqi_health_db

# JWT配置
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long
JWT_EXPIRES_IN=7d

# PDF配置
PDF_STORAGE_PATH=./reports

# 日志配置
LOG_LEVEL=info
LOG_FILE=./logs/app.log
```

```bash
# 保存并退出（Ctrl+O，Enter，Ctrl+X）
```

#### 步骤6：初始化数据库

```bash
# 创建数据库
mysql -u root -p
```

```sql
CREATE DATABASE yuanqi_health_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'yuanqi_user'@'localhost' IDENTIFIED BY 'your_secure_password_here';
GRANT ALL PRIVILEGES ON yuanqi_health_db.* TO 'yuanqi_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

```bash
# 导入数据库架构
mysql -u yuanqi_user -p yuanqi_health_db < database_schema.sql

# 执行数据迁移
node data_migration.js
```

#### 步骤7：配置Nginx反向代理

```bash
# 创建Nginx配置文件
sudo nano /etc/nginx/sites-available/yuanqi-api
```

配置内容：

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # 静态文件（前端）
    location / {
        root /var/www/yuanqi-frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API请求（后端）
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # PDF报告文件
    location /reports/ {
        alias /var/www/yuanqi-api/reports/;
        autoindex off;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/yuanqi-api /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

#### 步骤8：启动后端服务

```bash
cd /var/www/yuanqi-api

# 使用PM2启动
pm2 start backend-api-server.js --name yuanqi-api

# 保存PM2配置
pm2 save

# 设置开机自启
pm2 startup
# 按照提示执行输出的命令
```

#### 步骤9：配置SSL证书（HTTPS）

```bash
# 安装Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取SSL证书
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 按照提示输入邮箱地址并同意条款

# 自动续期
sudo certbot renew --dry-run
```

#### 步骤10：上传前端文件

```bash
# 创建前端目录
sudo mkdir -p /var/www/yuanqi-frontend
cd /var/www/yuanqi-frontend

# 上传以下文件：
# - health_assessment_final_fixed.html
# - health_assessment_backend_complete.html
# - frontend-api-integration.js
# - admin-api-integration.js
# - 所有品牌图片文件
```

**重要：** 修改前端配置中的API地址

```javascript
// frontend-api-integration.js
const API_CONFIG = {
    baseURL: 'https://yourdomain.com', // 修改为您的域名
    // ...
};

// admin-api-integration.js
const AdminAPIConfig = {
    baseURL: 'https://yourdomain.com', // 修改为您的域名
    // ...
};
```

#### 步骤11：防火墙配置

```bash
# 配置UFW防火墙
sudo ufw allow 22/tcp      # SSH
sudo ufw allow 80/tcp      # HTTP
sudo ufw allow 443/tcp     # HTTPS
sudo ufw deny 3306/tcp     # MySQL（仅本地）
sudo ufw enable
```

#### 步骤12：测试部署

```bash
# 检查后端服务状态
pm2 status
pm2 logs yuanqi-api

# 检查Nginx状态
sudo systemctl status nginx

# 检查MySQL状态
sudo systemctl status mysql

# 测试API
curl https://yourdomain.com/api/stats
```

在浏览器中访问：
- 前端：`https://yourdomain.com/health_assessment_final_fixed.html`
- 后台：`https://yourdomain.com/health_assessment_backend_complete.html`

---

### 方案B：适用于虚拟主机（cPanel/宝塔面板）

**适用场景：** 您的主机提供控制面板，但不支持Node.js

#### 限制说明
如果您的虚拟主机**不支持Node.js**，您有两个选择：

**选择1：降级方案（仅前端）**
- 使用前端降级功能，保留JSON文件下载功能
- 手动处理JSON文件
- 不使用后端API

**选择2：迁移到支持Node.js的主机**
- 升级到VPS服务器
- 或更换支持Node.js的虚拟主机

#### 宝塔面板部署（推荐）

如果您的服务器使用宝塔面板：

```bash
# 安装宝塔面板（如果未安装）
wget -O install.sh http://download.bt.cn/install/install-ubuntu_6.0.sh
sudo bash install.sh

# 登录宝塔面板
# 通常在 http://your_server_ip:8888
```

**宝塔面板操作步骤：**

1. **安装软件环境**
   - 软件商店 → 安装 Nginx 1.20+
   - 软件商店 → 安装 MySQL 5.7+
   - 软件商店 → 安装 Node.js 版本管理器

2. **创建网站**
   - 网站 → 添加站点
   - 域名：yourdomain.com
   - PHP版本：纯静态

3. **上传文件**
   - 文件 → 访问网站根目录
   - 上传所有前端文件
   - 上传backend-api-server.js等后端文件到 `/www/wwwroot/yourdomain.com/api/`

4. **安装Node.js环境**
   - 软件商店 → Node.js版本管理器
   - 安装Node.js 18.x
   - 创建PM2应用
     - 名称：yuanqi-api
     - 运行目录：/www/wwwroot/yourdomain.com/api/
     - 启动文件：backend-api-server.js
     - 端口：3000

5. **配置Nginx**
   - 网站 → 设置 → 配置文件
   - 添加反向代理规则：

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

6. **初始化数据库**
   - 数据库 → 添加数据库
   - 在phpMyAdmin中导入 `database_schema.sql`
   - 通过SSH执行 `node data_migration.js`

---

### 方案C：容器化部署（Docker）

**适用场景：** 需要更灵活、可移植的部署方式

#### 步骤1：创建Dockerfile

在项目根目录创建 `Dockerfile`:

```dockerfile
# 基础镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm install --production

# 复制应用文件
COPY . .

# 创建reports目录
RUN mkdir -p /app/reports

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["node", "backend-api-server.js"]
```

#### 步骤2：创建docker-compose.yml

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: yuanqi-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: yuanqi_health_db
      MYSQL_USER: yuanqi_user
      MYSQL_PASSWORD: yuanqi_password
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./database_schema.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

  api:
    build: .
    container_name: yuanqi-api
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_HOST: mysql
      DB_USER: yuanqi_user
      DB_PASSWORD: yuanqi_password
      DB_NAME: yuanqi_health_db
      JWT_SECRET: your_jwt_secret_key
    depends_on:
      - mysql
    volumes:
      - ./reports:/app/reports
      - ./logs:/app/logs
    restart: always

  nginx:
    image: nginx:alpine
    container_name: yuanqi-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./frontend:/usr/share/nginx/html
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - api
    restart: always

volumes:
  mysql-data:
```

#### 步骤3：创建nginx.conf

```nginx
events {
    worker_connections 1024;
}

http {
    upstream api {
        server api:3000;
    }

    server {
        listen 80;
        server_name yourdomain.com;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
```

#### 步骤4：构建和启动

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f api

# 执行数据库迁移
docker-compose exec api node data_migration.js
```

---

## 📁 需要上传的文件清单

### 后端文件（上传到服务器 `/var/www/yuanqi-api/`）

```
backend-api-server.js          # 后端API服务
database_schema.sql            # 数据库架构
data_migration.js               # 数据迁移脚本
package.json                   # 依赖配置
.env                           # 环境变量（需在服务器上创建）
```

### 前端文件（上传到 `/var/www/yuanqi-frontend/`）

```
health_assessment_final_fixed.html          # 评估页面
health_assessment_backend_complete.html      # 管理后台
frontend-api-integration.js                 # 前端API集成
admin-api-integration.js                    # 后台API集成
```

### 静态资源（品牌图片等）

```
元炁源流横版金色.png
元炁源流横版黑红色.png
元炁源流竖版金色.png
元炁源流竖版黑红色.png
```

### 数据文件（用于初始化）

```
symptoms_299_complete.json
first_assessment_20251213.json
second_assessment_20260113.json
sample_assessment_data.json
```

---

## 🔧 常见问题

### Q1: 我的虚拟主机不支持Node.js怎么办？

**A:** 您有以下选择：
1. 降级使用前端JSON下载功能
2. 升级到VPS服务器（推荐）
3. 使用Docker容器部署
4. 更换支持Node.js的虚拟主机

### Q2: 如何知道我的服务器是否支持Node.js？

**A:** 通过SSH连接服务器，执行：
```bash
node --version
# 如果显示版本号，说明已安装
# 如果显示"command not found"，说明未安装
```

### Q3: 我没有SSH访问权限怎么办？

**A:** 您可以通过以下方式解决：
1. 使用FTP/SFTP工具上传文件
2. 使用控制面板的文件管理器
3. 联系主机提供商开启SSH权限

### Q4: 如何获取SSL证书？

**A:** 使用免费的Let's Encrypt：
```bash
sudo apt install certbot
sudo certbot --nginx -d yourdomain.com
```

### Q5: 部署后无法访问怎么办？

**A:** 检查清单：
1. 确认防火墙已开放80、443端口
2. 检查Nginx配置：`sudo nginx -t`
3. 检查PM2进程：`pm2 status`
4. 查看错误日志：`pm2 logs yuanqi-api`
5. 检查域名DNS是否正确解析

---

## 📞 需要帮助？

如果部署过程中遇到问题，请提供以下信息：

1. 您的服务器类型（VPS/虚拟主机/容器）
2. 您使用的控制面板（cPanel/宝塔面板/其他）
3. 遇到的具体错误信息
4. 您的操作系统版本

这样我可以为您提供更精准的解决方案。
