# 元炁源流健康评估系统 - 完整升级实施指南

## 📋 目录

1. [系统概述](#系统概述)
2. [技术架构](#技术架构)
3. [环境准备](#环境准备)
4. [后端部署](#后端部署)
5. [数据库初始化](#数据库初始化)
6. [数据迁移](#数据迁移)
7. [前端改造](#前端改造)
8. [系统测试](#系统测试)
9. [故障排除](#故障排除)
10. [维护指南](#维护指南)

---

## 系统概述

本升级方案将元炁源流健康评估系统从纯前端JSON文件存储升级为完整的**后端API + MySQL数据库**架构，实现以下核心功能：

- ✅ 客户数据通过API提交到服务器
- ✅ 数据自动存储到MySQL数据库
- ✅ 智能健康分析引擎
- ✅ PDF报告自动生成
- ✅ 管理后台通过API获取数据
- ✅ 用户认证与权限管理
- ✅ 数据统计与趋势分析

---

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                             │
├───────────────────────────┬─────────────────────────────────┤
│  前端评估页面              │  管理后台                       │
│  health_assessment_*.html │  health_assessment_backend.html │
│  + frontend-api-integration.js                         │
│                           │  + admin-api-integration.js    │
└───────────────────────────┴─────────────────────────────────┘
                                 │
                                 │ HTTP/HTTPS
                                 │ RESTful API
                                 ▼
┌─────────────────────────────────────────────────────────────┐
│                        应用层                                │
│                   Node.js + Express                         │
│                  backend-api-server.js                       │
├─────────────────────────────────────────────────────────────┤
│  API端点:                                                   │
│  • POST /api/submit           - 提交评估                   │
│  • GET  /api/assessments      - 查询评估列表               │
│  • GET  /api/assessments/:id  - 查询评估详情               │
│  • POST /api/reports/generate/:id - 生成报告              │
│  • GET  /api/reports/:id      - 下载报告                   │
│  • POST /api/auth/login       - 用户登录                   │
│  • GET  /api/stats            - 获取统计数据               │
└─────────────────────────────────────────────────────────────┘
                                 │
                                 │ TCP
                                 ▼
┌─────────────────────────────────────────────────────────────┐
│                       数据层                                 │
│                      MySQL 数据库                           │
│                    database_schema.sql                      │
├─────────────────────────────────────────────────────────────┤
│  核心表:                                                    │
│  • users              - 用户表                              │
│  • assessments        - 评估记录表                          │
│  • assessment_symptoms - 评估症状关联表                   │
│  • symptom_library    - 症状库表                            │
│  • analysis_results   - 分析结果表                         │
│  • health_plans       - 调理方案表                          │
│  • reports            - 报告表                              │
│  • system_config      - 系统配置表                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 环境准备

### 1. 系统要求

| 组件 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Ubuntu 20.04+ / CentOS 7+ / Windows 10+ | Ubuntu 22.04 LTS |
| CPU | 2核心 | 4核心+ |
| 内存 | 2GB | 4GB+ |
| 存储 | 20GB | 50GB+ |
| Node.js | v14.x | v18.x LTS |
| MySQL | v5.7 | v8.0+ |
| Nginx | v1.18+ | v1.22+ |

### 2. 软件安装

#### 安装 Node.js (Ubuntu/Debian)

```bash
# 使用NodeSource仓库安装Node.js 18.x LTS
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

#### 安装 Node.js (CentOS/RHEL)

```bash
# 使用NodeSource仓库安装Node.js 18.x LTS
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node --version
npm --version
```

#### 安装 MySQL (Ubuntu/Debian)

```bash
# 安装MySQL Server
sudo apt update
sudo apt install -y mysql-server

# 启动MySQL服务
sudo systemctl start mysql
sudo systemctl enable mysql

# 安全配置
sudo mysql_secure_installation
```

#### 安装 MySQL (CentOS/RHEL)

```bash
# 安装MySQL Server
sudo yum install -y mysql-server

# 启动MySQL服务
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 获取临时密码
sudo grep 'temporary password' /var/log/mysqld.log

# 安全配置
sudo mysql_secure_installation
```

#### 安装 Nginx

```bash
# Ubuntu/Debian
sudo apt install -y nginx

# CentOS/RHEL
sudo yum install -y nginx

# 启动服务
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 安装 PM2 (进程管理)

```bash
npm install -g pm2

# 验证安装
pm2 --version
```

### 3. 网络配置

确保以下端口开放：

| 端口 | 协议 | 用途 |
|------|------|------|
| 80 | TCP | HTTP (开发环境) |
| 443 | TCP | HTTPS (生产环境) |
| 3000 | TCP | Node.js API服务 (内部通信) |
| 3306 | TCP | MySQL数据库 (仅本地访问) |

**防火墙配置示例 (Ubuntu UFW):**

```bash
# 允许SSH
sudo ufw allow 22/tcp

# 允许HTTP和HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 限制MySQL仅本地访问
sudo ufw deny 3306/tcp

# 启用防火墙
sudo ufw enable
```

---

## 后端部署

### 1. 创建项目目录

```bash
# 创建项目目录
sudo mkdir -p /var/www/yuanqi-api
sudo chown -R $USER:$USER /var/www/yuanqi-api
cd /var/www/yuanqi-api
```

### 2. 上传项目文件

将以下文件上传到 `/var/www/yuanqi-api` 目录：

- `backend-api-server.js` - 后端API服务器
- `database_schema.sql` - 数据库架构
- `data_migration.js` - 数据迁移脚本
- `.env.example` - 环境变量示例
- `package.json` - 项目依赖

### 3. 安装依赖

```bash
cd /var/www/yuanqi-api

# 创建package.json（如果不存在）
cat > package.json << 'EOF'
{
  "name": "yuanqi-health-assessment-api",
  "version": "1.0.0",
  "description": "元炁源流健康评估系统 - 后端API",
  "main": "backend-api-server.js",
  "scripts": {
    "start": "node backend-api-server.js",
    "dev": "nodemon backend-api-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "morgan": "^1.10.0",
    "winston": "^3.10.0",
    "html-pdf": "^3.0.1",
    "ejs": "^3.1.9",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
EOF

# 安装依赖
npm install --production
```

### 4. 配置环境变量

```bash
# 创建.env文件
cat > .env << 'EOF'
# 服务器配置
PORT=3000
NODE_ENV=production

# 数据库配置
DB_HOST=localhost
DB_USER=yuanqi_user
DB_PASSWORD=your_secure_password_here
DB_NAME=yuanqi_health_db

# JWT配置
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# PDF配置
PDF_STORAGE_PATH=./reports

# 日志配置
LOG_LEVEL=info
LOG_FILE=./logs/app.log
EOF

# 设置文件权限
chmod 600 .env
```

**重要提示：** 
- 请务必修改 `DB_PASSWORD` 为强密码
- 请修改 `JWT_SECRET` 为随机字符串
- 生产环境中，JWT_SECRET应至少32个字符

**生成安全的JWT_SECRET:**

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

---

## 数据库初始化

### 1. 创建数据库和用户

```bash
# 登录MySQL
sudo mysql -u root -p

# 在MySQL命令行中执行
```

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS yuanqi_health_db 
  CHARACTER SET utf8mb4 
  COLLATE utf8mb4_unicode_ci;

-- 创建数据库用户
CREATE USER IF NOT EXISTS 'yuanqi_user'@'localhost' 
  IDENTIFIED BY 'your_secure_password_here';

-- 授予权限
GRANT ALL PRIVILEGES ON yuanqi_health_db.* 
  TO 'yuanqi_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 退出MySQL
EXIT;
```

### 2. 导入数据库架构

```bash
# 导入数据库架构
mysql -u yuanqi_user -p yuanqi_health_db < database_schema.sql

# 验证表是否创建成功
mysql -u yuanqi_user -p yuanqi_health_db -e "SHOW TABLES;"
```

预期输出：

```
+---------------------------+
| Tables_in_yuanqi_health_db |
+---------------------------+
| analysis_results          |
| assessment_symptoms       |
| assessments               |
| health_plans              |
| reports                   |
| symptom_library          |
| system_config             |
| users                     |
+---------------------------+
```

### 3. 初始化症状库数据

```bash
# 检查symptoms_299_complete.json文件是否存在
ls -la symptoms_299_complete.json

# 执行数据迁移（包含症状库导入）
node data_migration.js
```

迁移脚本将自动：
- 导入299个症状到 `symptom_library` 表
- 导入现有评估数据到相关表
- 创建默认管理员账户

---

## 数据迁移

### 1. 准备数据文件

确保以下JSON文件在项目目录中：

```
/var/www/yuanqi-api/
├── symptoms_299_complete.json         # 症状库数据（299个症状）
├── first_assessment_20251213.json    # 评估数据示例1
├── second_assessment_20260113.json   # 评估数据示例2
└── sample_assessment_data.json       # 评估数据示例3
```

### 2. 执行数据迁移

```bash
cd /var/www/yuanqi-api

# 执行迁移脚本
node data_migration.js
```

**预期输出示例：**

```
========================================
  元炁源流健康评估系统 - 数据迁移工具
========================================
✓ 数据库连接成功

========== 开始迁移症状库 ==========

处理文件: symptoms_299_complete.json
症状数量: 299
  ✓ 导入: 头疼 (ID: 1)
  ✓ 导入: 白发 (ID: 2)
  ...
  ✓ 导入: 失眠 (ID: 299)

✓ symptoms_299_complete.json 迁移完成: 成功 299 条，跳过 0 条

========== 开始迁移评估数据 ==========

处理文件: first_assessment_20251213.json
✓ 创建评估记录 (ID: 1)
✓ 插入症状: 34 条
✓ 插入分析结果 (评分: 72)
✓ 插入调理方案

✓ first_assessment_20251213.json 迁移完成

========== 创建默认管理员账户 ==========
✓ 默认管理员账户创建成功
  用户名: admin
  密码: admin123
  ⚠ 请登录后立即修改密码！

========== 迁移统计 ==========
✓ 症状库记录: 299 条
✓ 评估记录: 3 条
✓ 评估症状: 102 条
✓ 分析结果: 3 条
✓ 用户记录: 1 条

========================================
  ✓ 数据迁移完成！
========================================
```

### 3. 验证数据迁移

```bash
# 登录MySQL验证
mysql -u yuanqi_user -p yuanqi_health_db

# 在MySQL命令行中执行
```

```sql
-- 查看症状数量
SELECT COUNT(*) as symptom_count FROM symptom_library;

-- 查看评估记录数量
SELECT COUNT(*) as assessment_count FROM assessments;

-- 查看最新的评估记录
SELECT * FROM assessments ORDER BY id DESC LIMIT 1;

-- 查看管理员用户
SELECT id, username, real_name, role, created_at 
FROM users WHERE role = 'admin';

EXIT;
```

---

## 前端改造

### 1. 前端评估页面改造

#### 步骤1: 引入API集成脚本

在 `health_assessment_final_fixed.html` 的 `</body>` 标签前添加：

```html
<!-- API集成脚本 -->
<script src="frontend-api-integration.js"></script>
```

#### 步骤2: 修改提交按钮

将原有的提交按钮改为调用API：

```html
<!-- 修改前 -->
<button id="submitBtn" onclick="downloadJSON()">提交评估</button>

<!-- 修改后 -->
<button id="submitAssessmentBtn">提交评估</button>
<!-- 保留降级方案 -->
<button id="downloadJSONBtn" style="display: none;">下载JSON文件</button>
```

#### 步骤3: 更新表单ID

确保表单有以下元素和ID：

```html
<!-- 基本信息 -->
<input type="text" id="realName" placeholder="姓名">
<input type="number" id="age" placeholder="年龄">
<input type="radio" name="gender" value="male"> 男
<input type="radio" name="gender" value="female"> 女
<input type="number" id="height" placeholder="身高">
<input type="number" id="weight" placeholder="体重">
<input type="number" id="waistCircumference" placeholder="腰围">
<input type="number" id="bloodSugar" placeholder="血糖">
<input type="number" id="systolicPressure" placeholder="收缩压">
<input type="number" id="diastolicPressure" placeholder="舒张压">
<textarea id="remarks" placeholder="备注"></textarea>

<!-- 症状选择区域 -->
<div id="symptomsContainer"></div>

<!-- 统计信息 -->
<div>已选择: <span id="selectedCount">0</span></div>
<div>总计: <span id="totalCount">299</span></div>
```

#### 步骤4: 配置API地址

编辑 `frontend-api-integration.js`，修改API配置：

```javascript
const API_CONFIG = {
    // 开发环境
    baseURL: 'http://localhost:3000',
    
    // 生产环境（取消注释并修改为实际域名）
    // baseURL: 'https://api.yourdomain.com',
    
    endpoints: {
        register: '/api/auth/register',
        login: '/api/auth/login',
        submit: '/api/assessments',
        getAssessment: '/api/assessments/',
        symptoms: '/api/symptoms'
    }
};
```

### 2. 管理后台改造

#### 步骤1: 引入API集成脚本

在 `health_assessment_backend_complete.html` 的 `</body>` 标签前添加：

```html
<!-- 后台API集成脚本 -->
<script src="admin-api-integration.js"></script>
```

#### 步骤2: 配置API地址

编辑 `admin-api-integration.js`，修改API配置：

```javascript
const AdminAPIConfig = {
    // 开发环境
    baseURL: 'http://localhost:3000',
    
    // 生产环境（取消注释并修改为实际域名）
    // baseURL: 'https://api.yourdomain.com',
    
    endpoints: {
        login: '/api/auth/login',
        assessments: '/api/assessments',
        assessmentDetail: '/api/assessments/',
        reports: '/api/reports',
        reportsGenerate: '/api/reports/generate/',
        analysis: '/api/analysis/',
        users: '/api/users',
        stats: '/api/stats'
    }
};
```

#### 步骤3: 添加页面元素

确保管理后台包含以下元素：

```html
<!-- 统计数据 -->
<div id="statTotal">0</div> <!-- 总评估数 -->
<div id="statToday">0</div> <!-- 今日评估 -->
<div id="statSymptoms">0</div> <!-- 症状数据库 -->
<div id="statScore">-</div> <!-- 平均评分 -->

<!-- 评估列表 -->
<div id="assessmentListContainer"></div>

<!-- 分页 -->
<div id="pagination"></div>

<!-- 详情模态框 -->
<div id="detailModal" style="display: none;">
    <div id="assessmentDetailContainer"></div>
    <button id="closeDetailBtn">关闭</button>
</div>

<!-- 搜索和筛选 -->
<input type="text" id="searchInput" placeholder="搜索姓名或手机号">
<select id="statusFilter">
    <option value="">全部状态</option>
    <option value="pending">待分析</option>
    <option value="analyzed">已分析</option>
    <option value="completed">已完成</option>
</select>
<button id="searchBtn">搜索</button>

<!-- 操作按钮 -->
<button id="refreshBtn">刷新</button>
<button id="logoutBtn">退出登录</button>
```

#### 步骤4: 添加样式

在HTML的`<style>`标签中添加以下样式：

```css
/* 评估列表样式 */
.assessment-item {
    background: #f9f9f9;
    border: 1px solid #e0e0e0;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 10px;
}

.assessment-item.selected {
    border: 2px solid #d4af37;
    background: #fffef5;
}

.assessment-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 10px;
}

.assessment-name {
    font-weight: bold;
    font-size: 16px;
}

.assessment-date {
    color: #999;
    font-size: 14px;
}

.assessment-status {
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 12px;
}

.status-pending {
    background: #fff3cd;
    color: #856404;
}

.status-analyzed {
    background: #cce5ff;
    color: #004085;
}

.status-completed {
    background: #d4edda;
    color: #155724;
}

.assessment-body {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.assessment-info span {
    margin-right: 15px;
    font-size: 14px;
    color: #666;
}

.assessment-actions {
    display: flex;
    gap: 8px;
}

.assessment-actions button {
    padding: 6px 12px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
}

.btn-view {
    background: #2196f3;
    color: white;
}

.btn-report {
    background: #d4af37;
    color: white;
}

.btn-download {
    background: #4caf50;
    color: white;
}

/* 详情模态框样式 */
#detailModal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10000;
}

#detailModal > div {
    background: white;
    padding: 30px;
    border-radius: 8px;
    max-width: 900px;
    max-height: 90vh;
    overflow-y: auto;
}

/* 分析图表样式 */
.chart-container {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.chart-item {
    display: flex;
    align-items: center;
    gap: 15px;
}

.chart-label {
    width: 100px;
    text-align: right;
    font-size: 14px;
}

.chart-bar-bg {
    flex: 1;
    height: 20px;
    background: #f0f0f0;
    border-radius: 4px;
    overflow: hidden;
}

.chart-bar {
    height: 100%;
    background: linear-gradient(90deg, #d4af37, #f4d03f);
    transition: width 0.3s ease;
}

.chart-value {
    width: 50px;
    font-size: 14px;
    font-weight: bold;
    color: #d4af37;
}

/* 分页样式 */
.pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 8px;
    margin-top: 20px;
}

.page-btn {
    padding: 8px 16px;
    border: 1px solid #ddd;
    background: white;
    border-radius: 4px;
    cursor: pointer;
}

.page-btn:hover {
    background: #f5f5f5;
}

.page-btn.active {
    background: #d4af37;
    color: white;
    border-color: #d4af37;
}

.page-btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.page-ellipsis {
    padding: 8px;
    color: #999;
}
```

---

## 系统测试

### 1. 启动后端服务

```bash
cd /var/www/yuanqi-api

# 开发模式（带自动重启）
npm run dev

# 生产模式
npm start

# 使用PM2管理进程（推荐）
pm2 start backend-api-server.js --name yuanqi-api
pm2 save
pm2 startup
```

**查看日志:**

```bash
# PM2日志
pm2 logs yuanqi-api

# 应用日志
tail -f logs/app.log
```

### 2. 测试API端点

#### 测试1: 用户登录

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

**预期响应:**

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "user_id": 1,
    "username": "admin",
    "real_name": "系统管理员",
    "role": "admin",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

#### 测试2: 获取症状列表

```bash
curl -X GET "http://localhost:3000/api/symptoms?page=1&size=10"
```

#### 测试3: 提交评估数据

创建测试文件 `test_assessment.json`:

```json
{
  "real_name": "测试用户",
  "age": 35,
  "gender": "male",
  "height": 175,
  "weight": 70,
  "waist_circumference": 85,
  "remarks": "测试评估",
  "symptoms": [
    {
      "symptom_id": 1,
      "symptom_name": "头疼",
      "side": "left",
      "intensity": 15,
      "severity": "moderate",
      "cause_labels": ["微循环", "肝"]
    }
  ]
}
```

提交评估:

```bash
curl -X POST http://localhost:3000/api/assessments \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d @test_assessment.json
```

#### 测试4: 查询评估列表

```bash
curl -X GET "http://localhost:3000/api/assessments?page=1&size=10" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

#### 测试5: 生成PDF报告

```bash
curl -X POST http://localhost:3000/api/reports/generate/1 \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### 3. 测试前端集成

#### 测试评估页面

1. 在浏览器中打开 `health_assessment_final_fixed.html`
2. 填写基本信息
3. 选择症状
4. 点击"提交评估"按钮
5. 验证是否成功提交并跳转到报告页面

#### 测试管理后台

1. 在浏览器中打开 `health_assessment_backend_complete.html`
2. 输入管理员账号密码（admin / admin123）
3. 验证是否能登录成功
4. 查看评估列表
5. 点击"查看详情"
6. 点击"生成报告"
7. 下载生成的PDF报告

---

## 故障排除

### 问题1: 数据库连接失败

**错误信息:**
```
Error: connect ECONNREFUSED 127.0.0.1:3306
```

**解决方案:**

```bash
# 检查MySQL服务是否运行
sudo systemctl status mysql

# 如果未运行，启动服务
sudo systemctl start mysql

# 检查MySQL端口是否监听
sudo netstat -tlnp | grep 3306

# 检查防火墙设置
sudo ufw status
```

### 问题2: JWT Token过期

**错误信息:**
```
{"code": 401, "message": "Token已过期"}
```

**解决方案:**

1. 重新登录获取新Token
2. 检查 `.env` 文件中的 `JWT_EXPIRES_IN` 配置
3. 清除浏览器localStorage中的旧Token

### 问题3: PDF生成失败

**错误信息:**
```
Error: Failed to generate PDF
```

**解决方案:**

```bash
# 检查reports目录是否存在且有写权限
mkdir -p reports
chmod 755 reports

# 检查是否安装了PDF生成依赖
npm list html-pdf
npm list ejs

# 如果缺失，重新安装
npm install html-pdf ejs
```

### 问题4: 前端CORS错误

**错误信息:**
```
Access to XMLHttpRequest at 'http://localhost:3000/api/...' 
from origin 'http://localhost:8080' has been blocked by CORS policy
```

**解决方案:**

确保后端服务器已启用CORS：

```javascript
// backend-api-server.js
const cors = require('cors');

app.use(cors({
    origin: ['http://localhost:8080', 'https://yourdomain.com'],
    credentials: true
}));
```

### 问题5: PM2进程崩溃

**错误信息:**
```
App [yuanqi-api] with id [0] exited with code [1]
```

**解决方案:**

```bash
# 查看PM2日志
pm2 logs yuanqi-api --lines 100

# 重启服务
pm2 restart yuanqi-api

# 查看详细信息
pm2 show yuanqi-api

# 如果问题持续，检查Node.js版本和依赖
node --version
npm list
```

---

## 维护指南

### 1. 日志管理

```bash
# 查看应用日志
tail -f /var/www/yuanqi-api/logs/app.log

# 日志轮转配置（使用logrotate）
sudo vim /etc/logrotate.d/yuanqi-api
```

添加以下内容：

```
/var/www/yuanqi-api/logs/*.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
    create 0644 www-data www-data
    sharedscripts
    postrotate
        pm2 reload yuanqi-api
    endscript
}
```

### 2. 数据库备份

创建备份脚本 `/var/www/yuanqi-api/backup.sh`:

```bash
#!/bin/bash

# 配置
BACKUP_DIR="/var/backups/yuanqi-db"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="yuanqi_health_db"
DB_USER="yuanqi_user"
DB_PASS="your_password"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u $DB_USER -p$DB_PASS $DB_NAME > $BACKUP_DIR/backup_$DATE.sql

# 压缩备份文件
gzip $BACKUP_DIR/backup_$DATE.sql

# 删除7天前的备份
find $BACKUP_DIR -name "backup_*.sql.gz" -mtime +7 -delete

echo "Backup completed: backup_$DATE.sql.gz"
```

设置定时任务:

```bash
# 编辑crontab
crontab -e

# 每天凌晨2点执行备份
0 2 * * * /var/www/yuanqi-api/backup.sh >> /var/log/yuanqi-backup.log 2>&1
```

### 3. 数据库优化

```bash
# 登录MySQL
mysql -u yuanqi_user -p yuanqi_health_db

# 执行优化
```

```sql
-- 分析表
ANALYZE TABLE assessments;
ANALYZE TABLE assessment_symptoms;
ANALYZE TABLE analysis_results;

-- 优化表
OPTIMIZE TABLE assessments;
OPTIMIZE TABLE assessment_symptoms;

-- 检查索引
SHOW INDEX FROM assessments;
SHOW INDEX FROM assessment_symptoms;

-- 添加缺失的索引（如果需要）
CREATE INDEX idx_assessment_date ON assessments(assessment_date);
CREATE INDEX idx_assessment_status ON assessments(status);
CREATE INDEX idx_assessment_symptom ON assessment_symptoms(assessment_id, symptom_id);
```

### 4. 系统监控

安装监控工具:

```bash
# 使用PM2 Plus（推荐）
pm2 link [your_public_key] [your_secret_key]

# 或使用简单监控脚本
pm2 monit

# 查看资源使用情况
pm2 status
pm2 info yuanqi-api
```

### 5. 更新部署

```bash
# 备份当前版本
cd /var/www
tar -czf yuanqi-api-backup-$(date +%Y%m%d).tar.gz yuanqi-api

# 更新代码
cd /var/www/yuanqi-api
git pull  # 或使用其他方式更新代码

# 安装新依赖
npm install --production

# 重启服务
pm2 restart yuanqi-api

# 验证服务状态
pm2 status
pm2 logs yuanqi-api --lines 50
```

---

## 附录

### A. API完整文档

详见 `API部署指南.md`

### B. 数据库架构说明

详见 `database_schema.sql` 中的注释

### C. 安全建议

1. **修改默认密码**
   ```bash
   # 登录MySQL后执行
   ALTER USER 'yuanqi_user'@'localhost' IDENTIFIED BY '新密码';
   ```

2. **使用HTTPS**
   ```bash
   # 使用Let's Encrypt免费SSL证书
   sudo certbot --nginx -d yourdomain.com
   ```

3. **限制数据库访问**
   ```bash
   # 确保MySQL仅监听本地
   sudo vim /etc/mysql/mysql.conf.d/mysqld.cnf
   # 修改 bind-address = 127.0.0.1
   ```

4. **定期更新**
   ```bash
   # 定期更新系统和依赖
   sudo apt update && sudo apt upgrade
   npm outdated
   npm update
   ```

### D. 联系支持

如有问题，请查阅以下文档：
- `数据库升级技术方案.md` - 技术方案详情
- `API部署指南.md` - API详细文档
- `最终使用指南.md` - 系统使用说明

---

## 结语

恭喜！您已成功完成元炁源流健康评估系统的后端API升级。

系统现在具备：
- ✅ 完整的数据库存储
- ✅ RESTful API接口
- ✅ 智能健康分析
- ✅ PDF报告生成
- ✅ 用户权限管理
- ✅ 数据统计功能

祝您使用愉快！
